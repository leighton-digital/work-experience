// Scenario: A student is writing a function to sum two numbers, but the function doesn't return the correct result.

// The function should return the concatenation of two strings, but it's not working correctly.
function concatenate(str1, str2) {
  return str1;
}

// Example usage:
// console.log(concatenate("hello", " world")); // Expected output: "hello world"
