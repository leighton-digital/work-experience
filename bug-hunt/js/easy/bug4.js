// Challenge 4: Check for Even Number
// Scenario: A student is writing a function to check if a number is even, but it always returns false.

// The function should return true if the number is even, but it's not working correctly.
function isEven(num) {
  if (num % 2 = 0) {
      return true;
  }
  return false;
}

// Example usage:
// console.log(isEven(4)); // Expected output: true
